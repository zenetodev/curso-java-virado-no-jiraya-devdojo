package academy.devdojo.maratonajava.introducao;

public class Operadores {
    public static void main(String[] args) {
        // Operadores aritméticos
        // int idade = 24;
        // boolean dentrodalei = idade >= 18 && idade <= 70;
        // boolean foradalei = idade < 18 || idade > 70; 
        
        // System.out.println("está dentro da lei: " + dentrodalei);
        // System.out.println("está fora da lei: " + foradalei);
                   
        int contador = 0;
        contador += 1; // contador = contador + 1;
        contador++; // contador += 1;
        contador--; // contador -= 1;
        System.out.println(contador);
        
    }
}
